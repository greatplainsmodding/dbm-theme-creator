    let FolderPath = path.join(__dirname, 'data', 'themes', 'New-Project.json');

    let options = {
        title: "DBM Theme Creator",
        defaultPath: FolderPath,
        buttonLabel: "Save",
        filters: [{
            name: 'json', extensions: ['json'],
        }]
    }

    let file = await dialog.showSaveDialog(newProject, options);
    if (file.canceled) return;

    let newProjectData = {
        "themeName": 'Test Theme',
        "themeAuthor": 'Great Plains Modding',
        "themeDescription": 'This is a test theme made with DBM Theme Creator.'
    }

    let data = JSON.stringify(newProjectData)
    fs.writeFileSync(file.filePath, data, 'utf-8');